services:
  - type: web
    name: ckg-mcp
    runtime: python
    buildCommand: pip install -e ".[serve]"
    startCommand: python -m ckg_mcp.serve
    plan: free
    envVars:
      - key: PORT
        generateValue: true
      - key: POSTHOG_KEY
        sync: false
      - key: RESEND_KEY
        sync: false
